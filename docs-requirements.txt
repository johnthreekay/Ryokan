# Pinned docs-build deps. Zensical reads `mkdocs.yml` as-is (no
# zensical.toml needed) so this is a single-line dep file. Major-
# version pin only; `pip install -r` resolves to the latest minor +
# patch on every CI run so security fixes land without a manual bump.
#
# Zensical is pre-1.0 (0.0.x) and ships on a fast cadence; the < 0.1
# pin will need a re-evaluation when 1.0 drops.
zensical>=0.0.57,<0.1
